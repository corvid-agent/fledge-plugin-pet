[plugin]
name = "fledge-plugin-pet"
version = "0.2.0"
description = "Corvid dev companion — powered by CorvidLabs/corvid-pet"
author = "CorvidLabs"

[[commands]]
name = "pet"
description = "Check on your corvid pet — feed it with dev activity"
binary = "target/release/fledge-pet"

[hooks]
build = "cargo build --release"
